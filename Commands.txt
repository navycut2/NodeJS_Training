
# git commands 
git pull nodeJS_origin master
git push nodeJS_origin master
git remote add origin git@github.com:navycut2/NodeJS_Training.git
git remote -v



Navigate to the folder where you want to create your project

lookup >> semantic versioning

lookup >> promise vs callbacks

Commands

1. npm init >>> first time project
2. npm install >> to install dependencies from a package.json m existing project
3. npm install express --save
4.npm ls --depth=0
5. npm -g ls --depth=0
6. npm install -g learnyounode >> node workshop
7. npm install -g expressworks >> express workshop


Popular Modules
{
    Express
    Grunt
    Gulp
    Mongoose
    Jade
    CSURF
}

going on about Express now :-
{
    npm install -g express
        $PATH > /\node_modules_global
    express expressApp
    install dependencies:
        > cd expressApp && npm install
    run the app:
        > SET DEBUG=expressApp:* & npm start
}

loopkup >> Express workings

example 1 : Flights API

/search  - dep and arrival query: cost, dept time

example 2: Movie ticket

/theatres

/shows

/ticket


reach out :- Kunal.mathur@tcs.com
759004